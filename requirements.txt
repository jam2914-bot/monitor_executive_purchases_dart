# OpenDart API 기반 임원 매수 모니터링 봇 필수 패키지

# HTTP 요청 처리
requests>=2.31.0

# 데이터 처리
pandas>=2.0.0

# 시간대 처리
pytz>=2023.3

# 환경 변수 처리 (기본 포함되지만 명시)
python-dotenv>=1.0.0

# JSON 처리 (기본 포함)
# json - 내장 모듈

# 로깅 (기본 포함)
# logging - 내장 모듈

# 정규표현식 (기본 포함)
# re - 내장 모듈

# 날짜/시간 처리 (기본 포함)
# datetime - 내장 모듈

# 데이터 클래스 (Python 3.7+)
# dataclasses - 내장 모듈

# 타입 힌팅 (기본 포함)
# typing - 내장 모듈

# 운영체제 인터페이스 (기본 포함)
# os - 내장 모듈

# 시스템 관련 (기본 포함)
# sys - 내장 모듈

# 시간 관련 (기본 포함)
# time - 내장 모듈
